{
  "probe_bank_version": "1",
  "probe_class": "instruction",
  "tasks": [
    {
      "id": "instruction-001",
      "prompt": "Reply with exactly two words, both lowercase, no punctuation.",
      "constraints": [
        {"type": "word_count", "n": 2},
        {"type": "lowercase_only"},
        {"type": "no_punctuation"}
      ]
    },
    {
      "id": "instruction-002",
      "prompt": "List exactly 3 fruits, one per line, with no numbering or bullets.",
      "constraints": [
        {"type": "line_count", "n": 3},
        {"type": "no_numbering_prefix"}
      ]
    },
    {
      "id": "instruction-003",
      "prompt": "Answer only 'yes' or 'no' to this question, nothing else: is 4 an even number?",
      "constraints": [
        {"type": "exact_match_ci", "values": ["yes", "no"]}
      ]
    }
  ]
}
