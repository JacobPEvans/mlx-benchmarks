# Throughput sweep via vllm benchmark_serving
#
# Usage (per model, per prompt_length):
#   python -m vllm.entrypoints.benchmark_serving \
#     --base-url http://localhost:11434 \
#     --model $MODEL \
#     --num-prompts 10 \
#     --input-len $PROMPT_LEN \
#     --output-len 256
#
# Run at each prompt_length listed below for a full sweep.
# Local models only — cloud endpoints not measured for throughput.

prompt_lengths = [256, 512, 1024]
output_length = 256
num_requests = 10
concurrency = 1
